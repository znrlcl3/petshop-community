<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.petshop.community.mapper.PostLikeMapper">

    <select id="existsByPostIdAndMemberId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM post_likes
        WHERE post_id = #{postId} AND member_id = #{memberId}
    </select>

    <insert id="insert">
        INSERT INTO post_likes (post_id, member_id, created_at)
        VALUES (#{postId}, #{memberId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM post_likes
        WHERE post_id = #{postId} AND member_id = #{memberId}
    </delete>
    
</mapper>